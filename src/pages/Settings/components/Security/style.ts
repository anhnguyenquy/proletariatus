import { makeStyles } from '@mui/styles'

export const useStyles = makeStyles({
  'security': {
    minHeight: '24.85rem',
    display: 'flex',
    flexDirection: 'column',
    paddingLeft: '7rem'
  }
})
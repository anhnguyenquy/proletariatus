import { makeStyles } from '@mui/styles'

export const useStyles = makeStyles({
  general: {
    minHeight: '24.85rem',
    display: 'flex',
    flexDirection: 'column',
    paddingLeft: '7rem'
  }
})
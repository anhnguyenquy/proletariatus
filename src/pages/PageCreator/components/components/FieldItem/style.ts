import { makeStyles } from "@mui/styles"

export const useStyles = makeStyles({
  'field-item': {
    backgroundColor: 'white',
    minHeight: '1rem',
    '& .field-item-title': {
      color: '#8A8A8A',
      fontSize: '0.8rem',
      margin: '0.5rem 0'
    }
  }
})

import { makeStyles } from "@mui/styles";

export const useStylesPC = makeStyles({
  'page-manager': {
    width: "100%",
    height: "100%",
    overflowX: "hidden",
    backgroundColor: "#F3F2EF"
    
  },
});
